import React, { FC } from 'react';
import CreatePost from './CreatePost';

const Posts: FC = () => {
  return (
    <div>
      <CreatePost />
      All posts :D
    </div>
  );
};

export default Posts;

export enum CodesStatuses {
  'OK' = 200,
  'Resource was created on server' = 201,
  'Reset content which sent this request' = 205,
  'Resource already exists' = 240,
  'Invalid syntax' = 400,
  'Unauthorized' = 401,
}
